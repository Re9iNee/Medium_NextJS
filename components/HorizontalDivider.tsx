import React, { ReactElement } from "react";

const HorizontalDivider = (): ReactElement => {
    return <hr className='max-w-lg mx-auto border border-yellow-500' />;
};

export default HorizontalDivider;
